import { Component } from '@angular/core';

@Component({
  selector: 'xng-root',
  template: `<xng-nx-welcome></xng-nx-welcome> `,
  styles: [''],
})
export class AppComponent {
  title = 'default';
}
