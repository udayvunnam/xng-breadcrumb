import { SkipBreadcrumbPipe } from './skip-breadcrumb.pipe';

describe('SkipBreadcrumbPipe', () => {
  it('create an instance', () => {
    const pipe = new SkipBreadcrumbPipe();
    expect(pipe).toBeTruthy();
  });
});
